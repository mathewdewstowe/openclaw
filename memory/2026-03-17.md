
## Model Fallback Rule
- Default: Opus (claude-opus-4-6)
- If "AI service unavailable" error occurs: switch to Sonnet 4.6 (claude-sonnet-4-6)

## Incident — n8n Down (05:58 UTC)
- n8n was not responding at 05:58 UTC heartbeat
- Cause: graceful SIGTERM shutdown (likely WSL restart or prior process kill)
- Restarted at ~05:59 UTC, confirmed 200 health check
- Logged to uptime_incidents in SQLite

## Dashboard Restart (11:43 UTC)
- Dashboard was down at heartbeat check (port 3737 not listening)
- Previous log showed it had been running but no process active
- Restarted: `node server.js` — confirmed 302 response
- Cause: process likely died (no crash log, just stopped)

## Session Bloat Note
- 9d3dec88 at 360KB (over 300KB threshold) but locked/active — can't prune until session ends

## Dashboard Updates (13:41 UTC)
- Added `completedAt` timestamp column to tasks — tracks when tasks are completed
- Fixed new tasks defaulting to Triage column (was inconsistently defaulting to "Later")
- Added instant UI feedback when ticking checkboxes (optimistic update before server round-trip)
- Fixed persistence issue: JSON reconciliation uses INSERT OR IGNORE, won't overwrite SQLite state
- Markdown task export now includes completion timestamps

## Camoufox Stealth Browser Installed (13:58 UTC)
- Installed `camoufox-stealth-browser` skill from ClawHub
- Installed Camoufox Python package + Firefox fork binary (~713MB)
- Created `scripts/camoufox-linkedin.py` — session manager (login/status/fetch)
- Updated LinkedIn skill to use Camoufox instead of Chrome CDP
- Profile stored at `~/.stealth-browser/profiles/linkedin/`
- Verified headless mode works — reached linkedin.com/login page
- ✅ First-time login completed via WSLg headed mode
- Profile slug: /in/me/ → Matthew James-Dewstowe
- Session persisted — headless mode confirmed working
- Browserbase also set up (project 8c5d74d3, context e8fa5ace) as cloud fallback — session closed, not primary
- LinkedIn skill updated to use Camoufox instead of Chrome CDP
- **Camoufox headless session persistence issue:** LinkedIn invalidates cookies when fingerprint changes between launches. Fixed fingerprint approach partially worked but still unreliable. Camoufox headed mode also crashed due to missing glxtest binary.
- **Chrome CDP is the reliable approach:** `chrome-easy-apply.py` works perfectly. Chrome launched from PowerShell with `--remote-debugging-port=9222 --user-data-dir=C:\Temp\chrome-debug`
- **4 Easy Apply submissions successful** (15:21-15:24 UTC):
  1. Product Owner – Record of Reference @ Hexaware Technologies ✅
  2. VP - Senior Product Security Engineer @ TechShack ✅
  3. Product Owner - Assets @ SoTalent ✅
  4. Head of Product (Maternity Cover) @ MacGregor Black ✅
- 3 jobs skipped (too many form steps / required fields not auto-fillable)
- Screenshots saved in jobs/screenshots/
